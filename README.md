# sm-rs-types

Generates Rust types from sm-json-data schemas.

To use, set `SM_JSON_DATA_SCHEMA_DIR` to point to `sm-json-data/schemas` (with
an absolute path). If not set, the (optional) submodule will be used.

The generated types are available in `sm_rs_types.types`.
